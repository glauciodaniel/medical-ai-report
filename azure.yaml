# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: medical-ai-report
metadata:
  template: medical-ai-report-backend@0.0.1-beta

services:
  medical-ai-backend:
    project: ./backend
    language: python
    host: containerapp
